import DEFAULT_STATE from './initialState';

const advanced = (state = DEFAULT_STATE.advancedDetail, action) => {
  switch (action.type) {
    default:
    return state;
  }
}

export default advanced;